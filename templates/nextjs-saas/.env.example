# Everything works locally - no external services needed!
# Just copy this to .env.local and you're ready to go

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
JWT_SECRET=local-dev-secret-key

# All authentication and payments work without any external services
# No API keys needed - everything is mocked for development

# When you're ready to upgrade to real services:
# CLERK_SECRET_KEY=your_clerk_secret_key
# STRIPE_SECRET_KEY=your_stripe_secret_key
